import scout

# The scout rig: Nav + Sonar + six Small Battery Holders (D-016). It
# surveys, ranks, validates and reports a shortlist of outpost sites -
# and stops there. It does not build. There is one Outpost Kit, so
# choosing where it goes is a conversation (D-013). See lib/scout.py.
scout.run(self)
